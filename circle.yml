machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker pull ubuntu:precise
    - cat Dockerfiles/Dockerfile.precise
    - docker build -f Dockerfiles/Dockerfile.precise .
