machine:
  services:
    - docker

dependencies:
  override:
    - docker -v
    - docker info
    - docker pull ubuntu:precise
    - ln -s Dockerfiles/Dockerfile.precise Dockerfile
    - docker build --pull=false .
