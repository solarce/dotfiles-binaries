machine:
  services:
    - docker

dependencies:
  override:
    - docker -v
    - docker info
    - docker pull solarce/dotfiles-ubuntu-precise
    - ln -s Dockerfiles/Dockerfile.precise Dockerfile

test:
  override:
    - docker build --pull=false .
